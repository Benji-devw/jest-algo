
describe('Pack demo', () => {

  it('Demo some', () => {
    const a = 2 + 2;
  
    expect(a).toBe(4)
  })
  test('Demo some', () => {
    const a = 2 + 2;
  
    expect(a).toBe(4)
  })
})

